version: "2"

sql:
  - schema: "./db/migration/"
    queries: "./db/query/"
    engine: "postgresql"
    gen:
      go:
        emit_json_tags: true
        emit_empty_slices: true
        emit_interface: true
        package: "db"
        out: "db/sqlc/"


